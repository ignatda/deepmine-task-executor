{
  "name": "deepmine-task-executor",
  "version": "1.0.0",
  "description": "test task for deepmine",
  "main": "server.ts",
  "scripts": {
    "start": "NODE_ENV=production && echo $NODE_ENV && node server.ts",
    "lint": "eslint .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ignatda/deepmine-task-executor.git"
  },
  "keywords": [],
  "author": "Dmitrii Sherstobitov",
  "license": "MIT",
  "dependencies": {
    "node-cron": "3.0.2"
  },
  "devDependencies": {
    "@types/node": "^18.13.0",
    "@types/node-cron": "^3.0.7",
    "typescript": "^4.9.5"
  }
}
